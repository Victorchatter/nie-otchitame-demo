apiVersion: v1
kind: ConfigMap
metadata:
  name: nie-otchitame-config
  namespace: nie-otchitame
data:
  # Backend settings (non-sensitive)
  BACKEND_PORT: "8000"
  LOG_LEVEL: "info"
  # Frontend runtime placeholders (replace with real runtime config if needed)
  VITE_API_BASE_URL: "/api/v1"
